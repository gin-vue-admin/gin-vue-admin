# M2 认证模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现登录/刷新/登出/当前用户四个认证端点，对齐前端 MSW 契约，建立 handler→service→repository 三层与 service 单测基础设施。

**Architecture:** Clean Architecture 三层 + 构造函数注入（main.go 手动组装）。service 依赖 repository 接口（仅 auth repo 抽接口，为单测）。纯 JWT 不落库，refresh 靠 TTL 自然过期。超管语义判断 `*` in permissions。错误统一 RFC 7807 ProblemDetail。

**Tech Stack:** Go 1.25、Gin v1.12、GORM v1.31、golang-jwt/v5 v5.3.1、bcrypt（golang.org/x/crypto v0.53.0）、testify、viper、zap。

**Spec:** `docs/superpowers/specs/2026-07-02-m2-auth-design.md`

## Global Constraints

- 工作目录基准：所有 `go` 命令在 `server/` 下执行（`cd server`）。
- MySQL 已运行：容器 `gva-mysql`，端口 13306，root/root，库 gva。
- 后端端口 8088（8080 被占用）。
- 代码注释用中文（与现有代码库一致）。
- 不主动执行 git 提交（用户全局规范）——计划中的 commit 步骤在执行时需用户确认；默认改为"准备提交，等用户确认"。
- 错误响应统一 RFC 7807 ProblemDetail；成功响应 `{code:0,data,msg}`。
- 登录失败统一文案"用户名或密码错误"，不区分用户不存在/密码错（防枚举）。
- bcrypt 密码上限 72 字节，binding 层 max=72 兜底。

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `server/internal/repository/user.go` | UserRepository 接口 + gorm 实现（FindByUsername/FindByID/FindByIDWithRoles/UpdateLoginStats） | 创建 |
| `server/internal/service/auth.go` | 认证业务：Login/Refresh/Logout/GetProfile/Seed；AuthResult/UserProfile 类型 | 创建 |
| `server/internal/service/auth_test.go` | service 单测 + mockUserRepo，10 用例 | 创建 |
| `server/internal/handler/auth.go` | LoginRequest/RefreshRequest + AuthHandler 四端点 | 创建 |
| `server/internal/middleware/auth.go` | AuthRequired(jwtMgr) 中间件 | 创建 |
| `server/internal/pkg/apperr/apperr.go` | 补 Write() + INTERNAL_ERROR 兜底 | 修改 |
| `server/internal/server/router.go` | NewRouter(authHandler, jwtMgr) 注册 /api/auth | 修改 |
| `server/cmd/api/main.go` | 组装依赖 + 调 Seed | 修改 |

依赖方向：handler → service → repository(interface) → model；service 另依赖 jwt.Manager、hash、config。

---

### Task 1: apperr 统一写出函数

**Files:**
- Modify: `server/internal/pkg/apperr/apperr.go`

**Interfaces:**
- Produces: `func Write(c *gin.Context, err error)` —— 后续所有 handler 错误路径统一调用。依赖 `response.Problem`、`response.WithCode`、`response.WithErrors`、`log.S`（均已存在）。

- [ ] **Step 1: 实现 Write**

在 `server/internal/pkg/apperr/apperr.go` 末尾追加。注意 import 需加 `github.com/gin-gonic/gin`、`gva/internal/pkg/response`、`gva/internal/pkg/log`：

```go
// Write 统一将错误写出为 HTTP 响应：应用错误按其 Status/Code 写 ProblemDetail，
// 未知错误记日志后回 500，不向前端泄漏内部细节。
func Write(c *gin.Context, err error) {
	if e, ok := As(err); ok {
		response.Problem(c, e.Status, e.Title, e.Detail,
			response.WithCode(e.Code),
			response.WithErrors(e.Errors),
		)
		return
	}
	log.S.Errorw("未预期错误", "err", err, "path", c.Request.URL.Path)
	response.Problem(c, 500, "", "服务暂时不可用",
		response.WithCode("INTERNAL_ERROR"),
	)
}
```

import 块最终应为：
```go
import (
	"errors"

	"github.com/gin-gonic/gin"
	"gva/internal/pkg/log"
	"gva/internal/pkg/response"
)
```

- [ ] **Step 2: 编译验证**

Run: `cd server && go build ./...`
Expected: 无输出，退出码 0。

- [ ] **Step 3: 准备提交（等用户确认）**

```bash
cd server
git add internal/pkg/apperr/apperr.go
git commit -m "feat(apperr): 统一错误写出函数 Write + INTERNAL_ERROR 兜底"
```
（按规范不自动提交，向用户确认后再执行。）

---

### Task 2: UserRepository 接口与实现

**Files:**
- Create: `server/internal/repository/user.go`

**Interfaces:**
- Consumes: `model.User`、`model.Role`、`model.Permission`（已有）、`*gorm.DB`（main.go 注入）。
- Produces:
  - `type UserRepository interface { FindByUsername(ctx, username) (*model.User, error); FindByID(ctx, id) (*model.User, error); FindByIDWithRoles(ctx, id) (*model.User, error); UpdateLoginStats(ctx, id) error }`
  - `func NewUserRepository(db *gorm.DB) UserRepository`
  - package 名 `repository`。

- [ ] **Step 1: 创建 repository/user.go**

```go
// Package repository 封装数据访问。仅对含分支业务逻辑的领域抽接口（为单测可注入 mock）。
package repository

import (
	"context"
	"time"

	"gorm.io/gorm"
	"gva/internal/model"
)

// UserRepository 用户数据访问接口。
// 抽接口目的：auth service 含登录/刷新等分支逻辑，需在单测中用 mock 替换 DB。
type UserRepository interface {
	FindByUsername(ctx context.Context, username string) (*model.User, error)
	FindByID(ctx context.Context, id uint) (*model.User, error)
	FindByIDWithRoles(ctx context.Context, id uint) (*model.User, error)
	UpdateLoginStats(ctx context.Context, id uint) error
}

// userRepository gorm 实现。
type userRepository struct {
	db *gorm.DB
}

// NewUserRepository 构造用户仓储。
func NewUserRepository(db *gorm.DB) UserRepository {
	return &userRepository{db: db}
}

func (r *userRepository) FindByUsername(ctx context.Context, username string) (*model.User, error) {
	var u model.User
	if err := r.db.WithContext(ctx).Where("username = ?", username).First(&u).Error; err != nil {
		return nil, err
	}
	return &u, nil
}

func (r *userRepository) FindByID(ctx context.Context, id uint) (*model.User, error) {
	var u model.User
	if err := r.db.WithContext(ctx).First(&u, id).Error; err != nil {
		return nil, err
	}
	return &u, nil
}

// FindByIDWithRoles 预加载 Roles 及其 Permissions（双层），供 GetProfile 构造 profile。
func (r *userRepository) FindByIDWithRoles(ctx context.Context, id uint) (*model.User, error) {
	var u model.User
	err := r.db.WithContext(ctx).
		Preload("Roles.Permissions").
		First(&u, id).Error
	if err != nil {
		return nil, err
	}
	return &u, nil
}

// UpdateLoginStats 更新最后登录时间与登录次数。失败仅由调用方告警，不影响登录主流程。
func (r *userRepository) UpdateLoginStats(ctx context.Context, id uint) error {
	return r.db.WithContext(ctx).Model(&model.User{}).Where("id = ?", id).
		Updates(map[string]any{
			"last_login_at": time.Now(),
			"login_count":   gorm.Expr("login_count + 1"),
		}).Error
}
```

- [ ] **Step 2: 编译验证**

Run: `cd server && go build ./...`
Expected: 无输出，退出码 0。

- [ ] **Step 3: 准备提交（等用户确认）**

```bash
cd server
git add internal/repository/user.go
git commit -m "feat(repo): UserRepository 接口与 gorm 实现"
```

---

### Task 3: AuthService 业务层（先写 Seed，可独立测试）

**Files:**
- Create: `server/internal/service/auth.go`
- Create: `server/internal/service/auth_test.go`（本任务仅 Seed 测试，其余业务测试在 Task 5 补全）

**Interfaces:**
- Consumes: `repository.UserRepository`、`jwt.Manager`、`config.Config`、`hash`、`model.*`。
- Produces:
  - `type AuthService struct`、`func NewAuthService(repo, jwtMgr, cfg) *AuthService`
  - `func (s *AuthService) Seed(ctx) error`
  - `func (s *AuthService) Login/Refresh/Logout/GetProfile`（Task 4/5 实现，本任务先放方法签名或留到后续）

> 说明：为遵循 TDD 且让每个任务可独立编译，本任务实现 Seed + 其测试，并把 Login/Refresh/GetProfile/Logout 的方法**先以桩函数形式定义**（返回 error 占位），下一任务用 TDD 逐个替换。

- [ ] **Step 1: 写 Seed 的失败测试**

创建 `server/internal/service/auth_test.go`（package `service`）。先建最小 mockUserRepo 与构造辅助：

```go
package service

import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"gorm.io/driver/sqlite" // 注意：需先 go get
	"gorm.io/gorm"
	"gva/internal/config"
	"gva/internal/model"
	"gva/internal/pkg/jwt"
)

// newTestDB 用 SQLite 内存库建表，隔离真实 MySQL。
func newTestDB(t *testing.T) *gorm.DB {
	t.Helper()
	db, err := gorm.Open(sqlite.Open("file::memory:?cache=shared"), &gorm.Config{})
	require.NoError(t, err)
	require.NoError(t, model.AutoMigrate(db))
	return db
}

func newAuthSvc(t *testing.T) (*AuthService, *gorm.DB) {
	t.Helper()
	db := newTestDB(t)
	repo := NewUserRepositoryFromDBForTest(db) // 见下方说明
	jwtMgr := jwt.NewManager(config.JWTConfig{
		Secret: "test-secret", AccessTTL: 3600, RefreshTTL: 604800, Issuer: "gva-test",
	})
	return NewAuthService(repo, jwtMgr), db
}

func TestSeed_Idempotent(t *testing.T) {
	svc, _ := newAuthSvc(t)
	ctx := context.Background()

	require.NoError(t, svc.Seed(ctx))
	require.NoError(t, svc.Seed(ctx)) // 第二次不报错、不重复

	// 校验：2 用户、2 角色、2 权限
}
```

> **关键：** 为让 service 单测用 SQLite 真表（验证 FirstOrCreate 幂等与外键关系最稳），本计划测试采用「真实 SQLite 库 + 真 repository 实现」而非纯 mock。这比 mock 更能验证 Seed 的多表关系逻辑。因此需暴露一个测试用构造函数。**调整 Task 2 的设计：** `NewUserRepository` 已接受 `*gorm.DB`，测试可直接 `repository.NewUserRepository(db)`。所以 `newAuthSvc` 里应为：
```go
repo := repository.NewUserRepository(db)
```
（删除上面 `NewUserRepositoryFromDBForTest` 占位，改用真实构造函数。）

- [ ] **Step 2: 加 SQLite 依赖并确认测试失败**

Run:
```bash
cd server
go get github.com/stretchr/testify@latest
go get gorm.io/driver/sqlite@latest
go mod tidy
```

修复 `auth_test.go` import 为：
```go
import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"gorm.io/driver/sqlite"
	"gorm.io/gorm"
	"gva/internal/config"
	"gva/internal/model"
	"gva/internal/pkg/jwt"
	"gva/internal/repository"
)
```
并把 `newAuthSvc` 的 repo 行改为 `repo := repository.NewUserRepository(db)`，返回签名改 `(*AuthService, *gorm.DB)`。

补全校验断言（替换 TODO）：
```go
	// 用户
	var users []model.User
	db.Find(&users)
	assert.Len(t, users, 2)
	// 角色
	var roles []model.Role
	db.Find(&roles)
	assert.Len(t, roles, 2)
	// 权限
	var perms []model.Permission
	db.Find(&perms)
	assert.Len(t, perms, 2)
```

Run: `cd server && go test ./internal/service/...`
Expected: 编译失败（`AuthService.Seed` 未定义 / `NewAuthService` 签名不符）。

- [ ] **Step 3: 实现 AuthService 骨架 + Seed**

创建 `server/internal/service/auth.go`：

```go
// Package service 承载业务逻辑，依赖 repository 接口与 pkg 工具，不依赖 HTTP 层。
package service

import (
	"context"
	"errors"

	"gorm.io/gorm"
	"gva/internal/config"
	"gva/internal/model"
	"gva/internal/pkg/hash"
	"gva/internal/pkg/jwt"
	"gva/internal/repository"
)

// AuthResult 登录/刷新返回，对齐前端 AuthResult。
type AuthResult struct {
	AccessToken  string `json:"accessToken"`
	RefreshToken string `json:"refreshToken"`
	ExpiresIn    int    `json:"expiresIn"`
}

// UserProfile 当前用户信息，对齐前端 UserProfile / mock SafeUser。
type UserProfile struct {
	ID          uint     `json:"id"`
	Username    string   `json:"username"`
	Nickname    string   `json:"nickname"`
	Avatar      string   `json:"avatar"`
	Roles       []string `json:"roles"`
	Permissions []string `json:"permissions"`
}

// AuthService 认证业务。
type AuthService struct {
	repo   repository.UserRepository
	jwtMgr *jwt.Manager
}

// NewAuthService 构造认证服务。cfg 暂留作扩展（如后续读其他配置）。
func NewAuthService(repo repository.UserRepository, jwtMgr *jwt.Manager) *AuthService {
	return &AuthService{repo: repo, jwtMgr: jwtMgr}
}

// 种子数据定义（对齐前端 mock USERS）。
var seedAdmin = struct {
	username, password, nickname string
}{
	username: "admin", password: "123456", nickname: "Admin",
}
var seedNormal = struct {
	username, password, nickname string
}{
	username: "user", password: "123456", nickname: "User",
}

// Seed 幂等种入角色/权限/账户。用 FirstOrCreate，已有数据不清不删。
func (s *AuthService) Seed(ctx context.Context) error {
	// 1. 权限
	permAll := model.Permission{Code: "*", Name: "通配权限", Type: "api"}
	if err := firstOrCreatePerm(ctx, &permAll); err != nil {
		return err
	}
	permUserRead := model.Permission{Code: "user:read", Name: "用户查看", Type: "api"}
	if err := firstOrCreatePerm(ctx, &permUserRead); err != nil {
		return err
	}

	// 2. 角色（带权限关联）
	if err := s.seedRole(ctx, "super_admin", "超级管理员", []model.Permission{permAll}); err != nil {
		return err
	}
	if err := s.seedRole(ctx, "user", "普通用户", []model.Permission{permUserRead}); err != nil {
		return err
	}

	// 3. 账户
	if err := s.seedUser(ctx, seedAdmin.username, seedAdmin.password, seedAdmin.nickname, "super_admin"); err != nil {
		return err
	}
	if err := s.seedUser(ctx, seedNormal.username, seedNormal.password, seedNormal.nickname, "user"); err != nil {
		return err
	}
	return nil
}
```

注意：service 直接持有 `*gorm.DB` 会破坏「依赖接口」的初衷。但 Seed 需要操作 Role/Permission/RolePermission/User/UserRole 多表，把这些全塞进 UserRepository 接口会臃肿。**折中：AuthService 增加一个 `db *gorm.DB` 字段专供 Seed 用**。修订 `NewAuthService` 与结构体：

```go
type AuthService struct {
	repo   repository.UserRepository
	db     *gorm.DB       // 仅 Seed 使用（多表种子操作）
	jwtMgr *jwt.Manager
}

func NewAuthService(repo repository.UserRepository, db *gorm.DB, jwtMgr *jwt.Manager) *AuthService {
	return &AuthService{repo: repo, db: db, jwtMgr: jwtMgr}
}
```

更新 `newAuthSvc`：`return NewAuthService(repo, db, jwtMgr), db`。

补 Seed 辅助函数（同文件 auth.go）：

```go
// firstOrCreatePerm 按 code 查/建权限。
func firstOrCreatePerm(ctx context.Context, p *model.Permission) error {
	return nil // 占位，Step 4 替换
}

func (s *AuthService) seedRole(ctx context.Context, code, name string, perms []model.Permission) error {
	return nil // 占位
}

func (s *AuthService) seedUser(ctx context.Context, username, password, nickname, roleCode string) error {
	return nil // 占位
}
```

> 本步骤先放占位桩，确保编译通过；Step 4 在测试驱动下填实现。**注意：** 这是为「先让 Seed 测试能编译并失败」的中间态。

- [ ] **Step 4: 运行测试，确认 Seed 失败（断言不满足）**

Run: `cd server && go test ./internal/service/... -run TestSeed_Idempotent -v`
Expected: 编译通过，测试 FAIL（Seed 桩返回 nil 但库里无数据，`assert.Len(users,2)` 失败）。

- [ ] **Step 5: 实现 Seed 辅助函数**

替换三个占位函数为真实实现：

```go
// firstOrCreatePerm 按 code 查/建权限。
func firstOrCreatePerm(ctx context.Context, db *gorm.DB, p *model.Permission) error {
	return db.WithContext(ctx).Where(model.Permission{Code: p.Code}).FirstOrCreate(p).Error
}

// seedRole 按 code 查/建角色，并补齐角色-权限关联（去重）。
func (s *AuthService) seedRole(ctx context.Context, code, name string, perms []model.Permission) error {
	role := model.Role{Code: code, Name: name, Status: "active"}
	if err := s.db.WithContext(ctx).Where(model.Role{Code: code}).FirstOrCreate(&role).Error; err != nil {
		return err
	}
	// 关联权限：替换法（FirstOrCreate 后 Append 权限）
	if err := s.db.WithContext(ctx).Model(&role).Association("Permissions").Replace(perms); err != nil {
		return err
	}
	return nil
}

// seedUser 按 username 查/建用户并绑定角色。密码 bcrypt 哈希。
func (s *AuthService) seedUser(ctx context.Context, username, password, nickname, roleCode string) error {
	// 查角色
	var role model.Role
	if err := s.db.WithContext(ctx).Where(model.Role{Code: roleCode}).First(&role).Error; err != nil {
		return err
	}
	// 查/建用户（仅在新建时哈希密码）
	var user model.User
	result := s.db.WithContext(ctx).Where(model.User{Username: username}).First(&user)
	if errors.Is(result.Error, gorm.ErrRecordNotFound) {
		hashed, err := hash.Hash(password)
		if err != nil {
			return err
		}
		user = model.User{
			Username: username, Password: hashed, Nickname: nickname,
			Status: "active",
		}
		if err := s.db.WithContext(ctx).Create(&user).Error; err != nil {
			return err
		}
	}
	// 绑定角色（去重）
	if err := s.db.WithContext(ctx).Model(&user).Association("Roles").Replace(&role); err != nil {
		return err
	}
	return nil
}
```

同步修正 `Seed` 里调用签名：`firstOrCreatePerm(ctx, s.db, &permAll)`、`firstOrCreatePerm(ctx, s.db, &permUserRead)`。

- [ ] **Step 6: 运行测试，确认通过**

Run: `cd server && go test ./internal/service/... -run TestSeed_Idempotent -v`
Expected: PASS。

- [ ] **Step 7: 准备提交（等用户确认）**

```bash
cd server
git add internal/service/ go.mod go.sum
git commit -m "feat(auth): AuthService 骨架 + 幂等 Seed（含 SQLite 单测）"
```

---

### Task 4: Login 业务（TDD）

**Files:**
- Modify: `server/internal/service/auth.go`
- Modify: `server/internal/service/auth_test.go`

**Interfaces:**
- Consumes: `repository.UserRepository.FindByUsername`、`hash.Compare`、`jwt.Manager.GenerateAccess/GenerateRefresh/AccessTTLSeconds`、`apperr`。
- Produces: `func (s *AuthService) Login(ctx, username, password string) (*AuthResult, error)`

- [ ] **Step 1: 写 Login 失败测试**

在 `auth_test.go` 追加。先加一个 helper 跑 Seed 造数据：

```go
func setupSeeded(t *testing.T) *AuthService {
	t.Helper()
	svc, _ := newAuthSvc(t)
	require.NoError(t, svc.Seed(context.Background()))
	return svc
}
```

追加测试：
```go
func TestLogin_Success(t *testing.T) {
	svc := setupSeeded(t)
	res, err := svc.Login(context.Background(), "admin", "123456")
	require.NoError(t, err)
	assert.NotEmpty(t, res.AccessToken)
	assert.NotEmpty(t, res.RefreshToken)
	assert.Equal(t, 3600, res.ExpiresIn)
}

func TestLogin_WrongPassword(t *testing.T) {
	svc := setupSeeded(t)
	_, err := svc.Login(context.Background(), "admin", "wrong")
	require.Error(t, err)
	e, ok := apperr.As(err)
	require.True(t, ok)
	assert.Equal(t, 401, e.Status)
	assert.Contains(t, e.Detail, "用户名或密码错误")
}

func TestLogin_UserNotFound(t *testing.T) {
	svc := setupSeeded(t)
	_, err := svc.Login(context.Background(), "nobody", "123456")
	require.Error(t, err)
	// 防枚举：与密码错同一文案
	e, _ := apperr.As(err)
	assert.Contains(t, e.Detail, "用户名或密码错误")
}

func TestLogin_DisabledUser(t *testing.T) {
	svc, db := newAuthSvc(t)
	require.NoError(t, svc.Seed(context.Background()))
	// 禁用 admin
	db.Model(&model.User{}).Where("username = ?", "admin").Update("status", "disabled")
	_, err := svc.Login(context.Background(), "admin", "123456")
	require.Error(t, err)
	e, _ := apperr.As(err)
	assert.Equal(t, 401, e.Status)
}
```

import 追加 `"gva/internal/pkg/apperr"`。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd server && go test ./internal/service/... -run TestLogin -v`
Expected: 编译失败（`svc.Login` 未定义）。

- [ ] **Step 3: 实现 Login**

在 `auth.go` 追加。import 加 `"gva/internal/pkg/apperr"`、`"gorm.io/gorm"`：

```go
// Login 校验凭据并签发 access/refresh token。
// 安全：用户不存在与密码错返回同一文案，防用户枚举。
func (s *AuthService) Login(ctx context.Context, username, password string) (*AuthResult, error) {
	user, err := s.repo.FindByUsername(ctx, username)
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, apperr.Unauthorized("用户名或密码错误")
		}
		return nil, err
	}
	if user.Status != "active" {
		return nil, apperr.Unauthorized("账户已禁用")
	}
	if err := hash.Compare(user.Password, password); err != nil {
		return nil, apperr.Unauthorized("用户名或密码错误")
	}

	access, err := s.jwtMgr.GenerateAccess(user.ID, user.Username)
	if err != nil {
		return nil, err
	}
	refresh, err := s.jwtMgr.GenerateRefresh(user.ID, user.Username)
	if err != nil {
		return nil, err
	}

	// 异步更新登录统计：独立 context，失败仅告警，不阻塞登录。
	go func(uid uint) {
		if err := s.repo.UpdateLoginStats(context.Background(), uid); err != nil {
			log.S.Warnw("更新登录统计失败", "uid", uid, "err", err)
		}
	}(user.ID)

	return &AuthResult{
		AccessToken:  access,
		RefreshToken: refresh,
		ExpiresIn:    s.jwtMgr.AccessTTLSeconds(),
	}, nil
}
```

import 追加 `"gva/internal/pkg/log"`。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd server && go test ./internal/service/... -run TestLogin -v`
Expected: 4 个用例 PASS。

- [ ] **Step 5: 准备提交（等用户确认）**

```bash
git add internal/service/
git commit -m "feat(auth): Login 业务（密码校验+token签发+防枚举）"
```

---

### Task 5: Refresh / GetProfile / Logout 业务（TDD）

**Files:**
- Modify: `server/internal/service/auth.go`
- Modify: `server/internal/service/auth_test.go`

**Interfaces:**
- Consumes: `jwt.Manager.Parse`、`jwt.Claims`（UserID/Type）、`repository.FindByID/FindByIDWithRoles`、`apperr`。
- Produces:
  - `func (s *AuthService) Refresh(ctx, refreshToken string) (*AuthResult, error)`
  - `func (s *AuthService) GetProfile(ctx, userID uint) (*UserProfile, error)`
  - `func (s *AuthService) Logout(ctx) error`

- [ ] **Step 1: 写失败测试**

追加到 `auth_test.go`：
```go
func TestRefresh_Success(t *testing.T) {
	svc := setupSeeded(t)
	// 先登录拿 refresh
	res, err := svc.Login(context.Background(), "admin", "123456")
	require.NoError(t, err)
	// 刷新
	res2, err := svc.Refresh(context.Background(), res.RefreshToken)
	require.NoError(t, err)
	assert.NotEmpty(t, res2.AccessToken)
	assert.NotEmpty(t, res2.RefreshToken)
	assert.Equal(t, 3600, res2.ExpiresIn)
}

func TestRefresh_WithAccessTokenFails(t *testing.T) {
	svc := setupSeeded(t)
	res, err := svc.Login(context.Background(), "admin", "123456")
	require.NoError(t, err)
	// 用 access token 去刷新应失败
	_, err = svc.Refresh(context.Background(), res.AccessToken)
	require.Error(t, err)
	e, _ := apperr.As(err)
	assert.Equal(t, 401, e.Status)
}

func TestRefresh_InvalidTokenFails(t *testing.T) {
	svc := setupSeeded(t)
	_, err := svc.Refresh(context.Background(), "garbage")
	require.Error(t, err)
}

func TestGetProfile_NormalUser(t *testing.T) {
	svc := setupSeeded(t)
	// user 账户登录后取 id
	res, _ := svc.Login(context.Background(), "user", "123456")
	// 从 access token 解析 uid
	claims, err := svc.jwtMgr.Parse(res.AccessToken)
	require.NoError(t, err)
	prof, err := svc.GetProfile(context.Background(), claims.UserID)
	require.NoError(t, err)
	assert.Equal(t, "user", prof.Username)
	assert.Contains(t, prof.Roles, "user")
	assert.Contains(t, prof.Permissions, "user:read")
}

func TestGetProfile_SuperAdminWildcard(t *testing.T) {
	svc := setupSeeded(t)
	res, _ := svc.Login(context.Background(), "admin", "123456")
	claims, _ := svc.jwtMgr.Parse(res.AccessToken)
	prof, err := svc.GetProfile(context.Background(), claims.UserID)
	require.NoError(t, err)
	assert.Equal(t, []string{"*"}, prof.Permissions)
	assert.Contains(t, prof.Roles, "super_admin")
}

func TestLogout_NoOp(t *testing.T) {
	svc := setupSeeded(t)
	assert.NoError(t, svc.Logout(context.Background()))
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd server && go test ./internal/service/... -run "TestRefresh|TestGetProfile|TestLogout" -v`
Expected: 编译失败（方法未定义）。

- [ ] **Step 3: 实现 Refresh / GetProfile / Logout**

追加到 `auth.go`。import 加 `"gva/internal/pkg/jwt"`（类型引用 jwt.TypeRefresh）：

```go
// Refresh 校验 refresh token 并签发新的 token 对。
// 纯 JWT 不落库：旧 refresh 在 TTL 内仍可解析，无法真正删除（接受权衡）。
func (s *AuthService) Refresh(ctx context.Context, refreshToken string) (*AuthResult, error) {
	claims, err := s.jwtMgr.Parse(refreshToken)
	if err != nil || claims.Type != jwt.TypeRefresh {
		return nil, apperr.Unauthorized("Invalid refresh token")
	}
	user, err := s.repo.FindByID(ctx, claims.UserID)
	if err != nil || user.Status != "active" {
		return nil, apperr.Unauthorized("Invalid refresh token")
	}
	access, err := s.jwtMgr.GenerateAccess(user.ID, user.Username)
	if err != nil {
		return nil, err
	}
	refresh, err := s.jwtMgr.GenerateRefresh(user.ID, user.Username)
	if err != nil {
		return nil, err
	}
	return &AuthResult{
		AccessToken:  access,
		RefreshToken: refresh,
		ExpiresIn:    s.jwtMgr.AccessTTLSeconds(),
	}, nil
}

// GetProfile 构造当前用户信息，汇总角色码与权限码。
// 超管语义：权限集合含 "*" 时返回 ["*"]（前端 isSuperAdmin 短路）。
func (s *AuthService) GetProfile(ctx context.Context, userID uint) (*UserProfile, error) {
	user, err := s.repo.FindByIDWithRoles(ctx, userID)
	if err != nil {
		return nil, apperr.Unauthorized("未授权")
	}
	roles := make([]string, 0, len(user.Roles))
	permSet := make(map[string]struct{})
	for _, r := range user.Roles {
		roles = append(roles, r.Code)
		for _, p := range r.Permissions {
			permSet[p.Code] = struct{}{}
		}
	}
	perms := make([]string, 0, len(permSet))
	if _, ok := permSet["*"]; ok {
		perms = []string{"*"}
	} else {
		for code := range permSet {
			perms = append(perms, code)
		}
	}
	return &UserProfile{
		ID:          user.ID,
		Username:    user.Username,
		Nickname:    user.Nickname,
		Avatar:      user.Avatar,
		Roles:       roles,
		Permissions: perms,
	}, nil
}

// Logout 纯 JWT 模式下为空操作（前端清本地 storage）。
func (s *AuthService) Logout(ctx context.Context) error {
	return nil
}
```

- [ ] **Step 4: 运行全部 service 测试确认通过**

Run: `cd server && go test ./internal/service/... -v`
Expected: 所有用例 PASS（Seed + Login + Refresh + GetProfile + Logout）。

- [ ] **Step 5: 准备提交（等用户确认）**

```bash
git add internal/service/
git commit -m "feat(auth): Refresh/GetProfile/Logout 业务 + 超管通配语义"
```

---

### Task 6: AuthRequired 中间件

**Files:**
- Create: `server/internal/middleware/auth.go`

**Interfaces:**
- Consumes: `jwt.Manager`、`apperr`、`gin.Context`。
- Produces: `func AuthRequired(jwtMgr *jwt.Manager) gin.HandlerFunc`；向 context 注入 key `"userID"`(uint)、`"username"`(string)。

- [ ] **Step 1: 实现中间件**

创建 `server/internal/middleware/auth.go`：
```go
package middleware

import (
	"github.com/gin-gonic/gin"
	"gva/internal/pkg/apperr"
	"gva/internal/pkg/jwt"
)

// ContextKeyUserID / ContextKeyUsername 上下文键常量。
const (
	ContextKeyUserID   = "userID"
	ContextKeyUsername = "username"
)

// AuthRequired 校验 access token 并注入用户身份。仅 access token 可用，refresh 不行。
func AuthRequired(jwtMgr *jwt.Manager) gin.HandlerFunc {
	return func(c *gin.Context) {
		auth := c.GetHeader("Authorization")
		if len(auth) <= 7 || auth[:7] != "Bearer " {
			apperr.Write(c, apperr.Unauthorized("未授权"))
			return
		}
		tokenStr := auth[7:]
		claims, err := jwtMgr.Parse(tokenStr)
		if err != nil || claims.Type != jwt.TypeAccess {
			apperr.Write(c, apperr.Unauthorized("未授权"))
			return
		}
		c.Set(ContextKeyUserID, claims.UserID)
		c.Set(ContextKeyUsername, claims.Username)
		c.Next()
	}
}
```

- [ ] **Step 2: 编译验证**

Run: `cd server && go build ./...`
Expected: 退出码 0。

- [ ] **Step 3: 准备提交（等用户确认）**

```bash
git add internal/middleware/auth.go
git commit -m "feat(middleware): AuthRequired access token 校验中间件"
```

---

### Task 7: AuthHandler + 路由 + main 组装

**Files:**
- Create: `server/internal/handler/auth.go`
- Modify: `server/internal/server/router.go`
- Modify: `server/cmd/api/main.go`

**Interfaces:**
- Consumes: `*service.AuthService`、`*jwt.Manager`、`response`、`apperr`、middleware `AuthRequired`、`gin.Context` 的 `userID`。
- Produces: 可运行的 4 端点；HTTP 服务能启动并响应。

- [ ] **Step 1: 实现 AuthHandler**

创建 `server/internal/handler/auth.go`：
```go
package handler

import (
	"github.com/gin-gonic/gin"
	"gva/internal/middleware"
	"gva/internal/pkg/apperr"
	"gva/internal/pkg/response"
	"gva/internal/service"
)

type LoginRequest struct {
	Username string `json:"username" binding:"required,min=3,max=64"`
	Password string `json:"password" binding:"required,min=6,max=72"`
}

type RefreshRequest struct {
	RefreshToken string `json:"refreshToken" binding:"required"`
}

// AuthHandler 认证端点。
type AuthHandler struct {
	svc *service.AuthService
}

func NewAuthHandler(svc *service.AuthService) *AuthHandler {
	return &AuthHandler{svc: svc}
}

// Login POST /api/auth/sessions
func (h *AuthHandler) Login(c *gin.Context) {
	var req LoginRequest
	if err := c.ShouldBindJSON(&req); err != nil {
		apperr.Write(c, apperr.BadRequest("VALIDATION_ERROR", err.Error()))
		return
	}
	res, err := h.svc.Login(c.Request.Context(), req.Username, req.Password)
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, res)
}

// Refresh POST /api/auth/tokens/refresh
func (h *AuthHandler) Refresh(c *gin.Context) {
	var req RefreshRequest
	if err := c.ShouldBindJSON(&req); err != nil {
		apperr.Write(c, apperr.BadRequest("VALIDATION_ERROR", err.Error()))
		return
	}
	res, err := h.svc.Refresh(c.Request.Context(), req.RefreshToken)
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, res)
}

// Logout DELETE /api/auth/sessions
func (h *AuthHandler) Logout(c *gin.Context) {
	_ = h.svc.Logout(c.Request.Context())
	response.Success(c, nil)
}

// Me GET /api/auth/users/me
func (h *AuthHandler) Me(c *gin.Context) {
	uidAny, _ := c.Get(middleware.ContextKeyUserID)
	uid, _ := uidAny.(uint)
	prof, err := h.svc.GetProfile(c.Request.Context(), uid)
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, prof)
}
```

- [ ] **Step 2: 修改 router.go**

修改 `server/internal/server/router.go`：
- 签名改 `func NewRouter(authHandler *handler.AuthHandler, jwtMgr *jwt.Manager) *gin.Engine`
- import 加 `"gva/internal/middleware"`、`"gva/internal/pkg/jwt"`
- 在 `api := r.Group("/api")` 块内，health 路由后追加 auth 组：

```go
	// 认证
	auth := api.Group("/auth")
	{
		auth.POST("/sessions", authHandler.Login)
		auth.DELETE("/sessions", authHandler.Logout)
		auth.POST("/tokens/refresh", authHandler.Refresh)
		auth.GET("/users/me", middleware.AuthRequired(jwtMgr), authHandler.Me)
	}
```

完整 router.go 顶部 import 块：
```go
import (
	"github.com/gin-gonic/gin"
	"gva/internal/handler"
	"gva/internal/middleware"
	"gva/internal/pkg/jwt"
	"gva/internal/pkg/response"
)
```

- [ ] **Step 3: 修改 main.go 组装依赖**

修改 `server/cmd/api/main.go`：在 AutoMigrate 后、HTTP 启动前插入组装。import 加 `"context"`、`"gva/internal/repository"`、`"gva/internal/service"`、`"gva/internal/pkg/jwt"`。

将原「4. 启动 HTTP 服务」段替换为：
```go
	// 4. 组装依赖（构造注入）
	jwtMgr := jwt.NewManager(cfg.JWT)
	userRepo := repository.NewUserRepository(gdb)
	authSvc := service.NewAuthService(userRepo, gdb, jwtMgr)
	if err := authSvc.Seed(context.Background()); err != nil {
		log.L.Fatal("种子数据初始化失败", zap.Error(err))
	}
	log.L.Info("种子数据就绪")

	authHandler := handler.NewAuthHandler(authSvc)
	r := server.NewRouter(authHandler, jwtMgr)
```

- [ ] **Step 4: 编译验证**

Run: `cd server && go build ./...`
Expected: 退出码 0。

- [ ] **Step 5: 启动并端到端验收**

Run:
```bash
cd server
go run cmd/api/main.go > /tmp/gva-m2.log 2>&1 &
sleep 5
# 1. 登录
curl -s -X POST http://127.0.0.1:8088/api/auth/sessions -H 'Content-Type: application/json' -d '{"username":"admin","password":"123456"}'
# 2. 错误密码（应 401 ProblemDetail "用户名或密码错误"）
curl -s -X POST http://127.0.0.1:8088/api/auth/sessions -H 'Content-Type: application/json' -d '{"username":"admin","password":"wrong"}'
```
Expected:
- 登录返回 `{"code":0,"data":{"accessToken":"...","refreshToken":"...","expiresIn":3600},"msg":"ok"}`
- 错误密码返回 401 ProblemDetail，detail 含"用户名或密码错误"
- 日志含"种子数据就绪"

继续验收（用上一步拿到的 token）：
```bash
TOKEN=<上一步的 accessToken>
REF=<上一步的 refreshToken>
# 3. 刷新
curl -s -X POST http://127.0.0.1:8088/api/auth/tokens/refresh -H 'Content-Type: application/json' -d "{\"refreshToken\":\"$REF\"}"
# 4. me 带 token（admin 应 permissions:["*"]）
curl -s http://127.0.0.1:8088/api/auth/users/me -H "Authorization: Bearer $TOKEN"
# 5. me 不带 token（应 401）
curl -s http://127.0.0.1:8088/api/auth/users/me
# 6. user 账户登录后 me（应 permissions:["user:read"]）
curl -s -X POST http://127.0.0.1:8088/api/auth/sessions -H 'Content-Type: application/json' -d '{"username":"user","password":"123456"}'
```

Expected:
- 刷新返回新 token 对
- admin me：permissions 为 `["*"]`，roles 含 `super_admin`
- 不带 token：401 ProblemDetail
- user me：permissions 含 `user:read`

- [ ] **Step 6: 停掉测试进程**

```bash
pkill -f "go run cmd/api/main.go" 2>/dev/null; pkill -f "/main$" 2>/dev/null; echo done
```

- [ ] **Step 7: 跑全量单测**

Run: `cd server && go test ./...`
Expected: 全部 PASS。

- [ ] **Step 8: 准备提交（等用户确认）**

```bash
git add internal/handler/ internal/server/ cmd/api/main.go
git commit -m "feat(auth): AuthHandler + 路由 + 依赖组装（M2 完成）"
```

---

## Self-Review

**Spec coverage:** 逐项核对 spec 第 12 节验收标准：
- 编译通过 → Task 7 Step 4 ✓
- 日志含"种子数据就绪" → Task 7 Step 3/5 ✓
- 登录返回 token 对 → Task 7 Step 5 ✓
- 错密码 401 防枚举 → Task 7 Step 5 + Task 4 单测 ✓
- refresh 换新 token → Task 7 Step 5 + Task 5 单测 ✓
- me admin permissions:["*"] → Task 7 Step 5 + Task 5 单测 ✓
- me 不带 token 401 → Task 7 Step 5 ✓
- 10 用例单测 → Task 3/4/5 合计 Seed(1)+Login(4)+Refresh(3)+GetProfile(2)+Logout(1)=11 ✓（超 10）
- user me permissions:["user:read"] → Task 7 Step 5 + Task 5 单测 ✓

**Placeholder scan:** 计划中无 TODO/TBD；Task 3 Step 3 的桩函数在 Step 5 被真实实现替换，标注清晰。

**Type consistency:**
- `NewAuthService(repo, db, jwtMgr)` —— Task 3 定义，Task 7 调用一致 ✓
- `NewRouter(authHandler, jwtMgr)` —— Task 7 Step 2 定义，Step 3 调用一致 ✓
- `middleware.ContextKeyUserID` —— Task 6 定义，Task 7 handler 引用一致 ✓
- `jwt.TypeRefresh/TypeAccess` —— jwt 包已存在（Task 4/5/6 引用）✓
- `repository.NewUserRepository(db)` —— Task 2 定义，Task 3/7 调用一致 ✓

**潜在风险提示（非阻断）：**
1. SQLite 内存库与 MySQL 的 SQL 方言差异（如 `gorm.Expr`、`Association().Replace`）需在 Task 3 Step 6 实测；若 SQLite 不支持某写法，回退为 MySQL 测试库或调整实现。
2. `c.GetHeader` Bearer 解析用了固定长度切片，空 header 已由 `len(auth) <= 7` 防护。
3. 测试用 SQLite 共享内存库 `file::memory:?cache=shared` 可能跨测试串数据——若发现串扰，改为每个测试独立临时文件库。
