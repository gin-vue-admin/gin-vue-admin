# M3.2 角色模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 role 域 9 端点（CRUD + 列表/导出 + 权限分配子资源），复用 M3.1 通用基础设施，对齐前端 MSW 契约。

**Architecture:** 沿用 M3.1 三层（handler→service→repository 接口）+ 构造注入。复用 pagination/csvutil/middleware/apperr/testutil。权限分配子资源 API 用 code、表存 id（code↔id 转换）。删除事务内软删 + 解除关联（raw SQL 清 user_roles）。

**Tech Stack:** Go 1.25（泛型）、Gin v1.12、GORM v1.31、SQLite 测试、testify。

**Spec:** `docs/superpowers/specs/2026-07-02-m3-2-role-design.md`

## Global Constraints

- 工作目录基准：所有 go 命令在 `server/` 下（`cd server && ...`）。
- MySQL 容器 gva-mysql:13306（root/root/gva）已运行；后端端口 8088。
- 代码注释中文，与现有一致。
- **不主动执行 git 提交/建分支**（用户规范）——commit 步骤改为"准备提交，等用户确认"。
- 错误统一 RFC 7807 ProblemDetail（apperr.Write）；成功 `{code:0,data,msg}`。
- 超管语义：权限码集合含 `*` 短路放行。
- 时间字段 JSON：`createTime/updateTime`（base.Model 已对齐，M3.1 已改）。
- 导出：ApiResult 包裹 csv，msg="导出成功"。
- 软删除：Role 已有 DeletedAt；唯一索引 conflict 接受为 409。
- 权限缓存：permission/role CRUD 后调 `middleware.InvalidateAll()`。
- 校验错误用 `apperr.Validation(detail, nil)`（422，M3.1 已建）。
- isDuplicateKey 复用 M3.1 service 包的（同包可直接调用）。
- 提交身份：git config 已为 `如水 <rushui@qq.com>`，文档/命令不得含本机绝对路径或 wangtao。

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `server/internal/model/rbac.go` | Role 加 Description | 修改 |
| `server/internal/service/auth.go` | seedRole 补 Description | 修改 |
| `server/internal/repository/role.go` | RoleRepository 接口 + gorm 实现 | 创建 |
| `server/internal/service/role.go` | RoleService（CRUD+list+export+权限分配） | 创建 |
| `server/internal/service/role_test.go` | service 单测 | 创建 |
| `server/internal/handler/role.go` | RoleHandler（9端点） | 创建 |
| `server/internal/server/router.go` | 注册 /api/role 路由组 | 修改 |
| `server/cmd/api/main.go` | 组装 Role 依赖 | 修改 |

---

### Task 1: Role model 加 Description

**Files:**
- Modify: `server/internal/model/rbac.go`

**Interfaces:**
- Produces: `model.Role` 含 Description 字段。

- [ ] **Step 1: 加 Description 字段**

在 `server/internal/model/rbac.go` 的 `type Role struct` 中，在 `Name` 字段后加 `Description`：
```go
type Role struct {
	Model
	Code        string         `gorm:"uniqueIndex;size:64;not null" json:"code"`
	Name        string         `gorm:"size:64;not null" json:"name"`
	Description string         `gorm:"size:255" json:"description"`   // 新增：对齐前端 RoleInfo
	Status      string         `gorm:"size:16;default:active" json:"status"`
	Sort        int            `gorm:"default:0" json:"sort"`
	Remark      string         `gorm:"size:255" json:"remark"`
	DeletedAt   gorm.DeletedAt `gorm:"index" json:"-"`

	Permissions []Permission `gorm:"many2many:role_permissions;" json:"permissions,omitempty"`
}
```

- [ ] **Step 2: 编译验证**

Run: `cd server && go build ./...`
Expected: 退出码 0。

- [ ] **Step 3: 准备提交（等用户确认）**

```bash
cd server
git add internal/model/rbac.go
git commit -m "feat(model): Role 加 Description 字段对齐前端契约"
```

---

### Task 2: seedRole 补 Description

**Files:**
- Modify: `server/internal/service/auth.go`

**Interfaces:**
- Produces: `seedRole` 签名扩展为 `(ctx, code, name, description, perms)`，super_admin/user 角色带 Description。

- [ ] **Step 1: 扩展 seedRole 签名 + 调用**

在 `server/internal/service/auth.go` 中：

1. `seedRole` 函数签名从 `func (s *AuthService) seedRole(ctx context.Context, code, name string, perms []model.Permission) error` 改为：
```go
func (s *AuthService) seedRole(ctx context.Context, code, name, description string, perms []model.Permission) error {
	role := model.Role{Code: code, Name: name, Description: description, Status: "active"}
	if err := s.db.WithContext(ctx).Where(model.Role{Code: code}).FirstOrCreate(&role).Error; err != nil {
		return err
	}
	// 已存在角色补 Description（FirstOrCreate 不覆盖已存在记录的字段）
	if err := s.db.WithContext(ctx).Model(&role).Update("description", description).Error; err != nil {
		return err
	}
	if err := s.db.WithContext(ctx).Model(&role).Association("Permissions").Replace(perms); err != nil {
		return err
	}
	return nil
}
```
> 注：FirstOrCreate 对已存在记录不更新非查询字段，故额外 Update 补 Description。新建记录在 FirstOrCreate 时已写入 Description。

2. Seed 中两处调用改为带 Description：
```go
	if err := s.seedRole(ctx, "super_admin", "超级管理员", "超级管理员，拥有所有权限", []model.Permission{permAll}); err != nil {
		return err
	}
	if err := s.seedRole(ctx, "user", "普通用户", "普通用户，仅查看权限", []model.Permission{permUserRead}); err != nil {
		return err
	}
```

- [ ] **Step 2: 跑 M2/M3.1 回归**

Run: `cd server && go test ./internal/service/... -count=1 -run TestSeed`
Expected: PASS（Seed 幂等，补 Description 不破坏断言）。

- [ ] **Step 3: 准备提交（等用户确认）**

```bash
git add internal/service/auth.go
git commit -m "feat(auth): seedRole 补 Description"
```

---

### Task 3: RoleRepository（TDD）

**Files:**
- Create: `server/internal/repository/role.go`
- Create: `server/internal/repository/role_test.go`

**Interfaces:**
- Consumes: `model.Role`、`pagination.Query`、`*gorm.DB`。
- Produces: `RoleRepository` 接口 + `NewRoleRepository(db)`，含权限分配与删除解除关联方法。

- [ ] **Step 1: 写失败测试**

创建 `server/internal/repository/role_test.go`：
```go
package repository

import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"gva/internal/model"
	"gva/internal/pkg/pagination"
	"gva/internal/testutil"
)

func newRoleRepo(t *testing.T) RoleRepository {
	return NewRoleRepository(testutil.NewTestDB(t))
}

func TestRoleRepo_CRUD(t *testing.T) {
	repo := newRoleRepo(t)
	ctx := context.Background()
	r := &model.Role{Code: "r1", Name: "角色1", Description: "d", Status: "active"}
	require.NoError(t, repo.Create(ctx, r))
	assert.NotZero(t, r.ID)

	got, err := repo.FindByID(ctx, r.ID)
	require.NoError(t, err)
	assert.Equal(t, "r1", got.Code)

	got.Name = "改"
	require.NoError(t, repo.Update(ctx, got))

	_, err = repo.FindByID(ctx, 9999)
	assert.Error(t, err) // NotFound
}

func TestRoleRepo_List(t *testing.T) {
	repo := newRoleRepo(t)
	ctx := context.Background()
	for i := 0; i < 15; i++ {
		repo.Create(ctx, &model.Role{Code: "c", Name: "n", Status: "active"})
	}
	q := pagination.Query{Page: 1, Size: 10}
	q.Normalize()
	roles, total, err := repo.List(ctx, q)
	require.NoError(t, err)
	assert.Equal(t, int64(15), total)
	assert.Len(t, roles, 10)
}

func TestRoleRepo_Delete_ClearsAssociations(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := NewRoleRepository(db)
	ctx := context.Background()
	// 建角色 + 权限 + 关联 + user→role 关联
	role := &model.Role{Code: "r1", Name: "n", Status: "active"}
	repo.Create(ctx, role)
	perm := &model.Permission{Code: "p1", Name: "p", Type: "api", Status: "active"}
	db.Create(perm)
	db.Model(role).Association("Permissions").Replace(perm)
	user := &model.User{Username: "u1", Password: "x", Status: "active"}
	db.Create(user)
	db.Model(user).Association("Roles").Replace(role)

	require.NoError(t, repo.Delete(ctx, role.ID))

	// role 软删
	_, err := repo.FindByID(ctx, role.ID)
	assert.Error(t, err)
	// role_permissions 已清
	var rpCount int64
	db.Table("role_permissions").Where("role_id = ?", role.ID).Count(&rpCount)
	assert.Equal(t, int64(0), rpCount)
	// user_roles 已清
	var urCount int64
	db.Table("user_roles").Where("role_id = ?", role.ID).Count(&urCount)
	assert.Equal(t, int64(0), urCount)
}

func TestRoleRepo_PermissionCodesAndReplace(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := NewRoleRepository(db)
	ctx := context.Background()
	role := &model.Role{Code: "r1", Name: "n", Status: "active"}
	repo.Create(ctx, role)
	p1 := &model.Permission{Code: "p1", Name: "p", Type: "api", Status: "active"}
	p2 := &model.Permission{Code: "p2", Name: "p", Type: "api", Status: "active"}
	db.Create(p1)
	db.Create(p2)

	// Replace
	require.NoError(t, repo.ReplaceRolePermissions(ctx, role.ID, []uint{p1.ID, p2.ID}))
	codes, err := repo.GetRolePermissionCodes(ctx, role.ID)
	require.NoError(t, err)
	assert.ElementsMatch(t, []string{"p1", "p2"}, codes)

	// FindPermissionIDsByCodes
	m, err := repo.FindPermissionIDsByCodes(ctx, []string{"p1", "p2", "unknown"})
	require.NoError(t, err)
	assert.Contains(t, m, "p1")
	assert.Contains(t, m, "p2")
	assert.NotContains(t, m, "unknown") // 未知不在 map
}
```

- [ ] **Step 2: 跑测试确认 RED**

Run: `cd server && go test ./internal/repository/ -run TestRoleRepo -v`
Expected: 编译失败（NewRoleRepository 未定义）。

- [ ] **Step 3: 实现 role.go**

创建 `server/internal/repository/role.go`：
```go
package repository

import (
	"context"

	"gorm.io/gorm"
	"gva/internal/model"
	"gva/internal/pkg/pagination"
)

// RoleRepository 角色数据访问接口。
type RoleRepository interface {
	List(ctx context.Context, q pagination.Query) ([]model.Role, int64, error)
	ListAll(ctx context.Context, q pagination.Query) ([]model.Role, error) // 不分页，导出用
	FindByID(ctx context.Context, id uint) (*model.Role, error)
	Create(ctx context.Context, r *model.Role) error
	Update(ctx context.Context, r *model.Role) error
	Delete(ctx context.Context, id uint) error // 事务内软删+解除关联
	BatchDelete(ctx context.Context, ids []uint) error
	GetRolePermissionCodes(ctx context.Context, roleID uint) ([]string, error)
	ReplaceRolePermissions(ctx context.Context, roleID uint, permissionIDs []uint) error
	FindPermissionIDsByCodes(ctx context.Context, codes []string) (map[string]uint, error)
}

type roleRepository struct {
	db *gorm.DB
}

func NewRoleRepository(db *gorm.DB) RoleRepository {
	return &roleRepository{db: db}
}

func applyRoleFilters(db *gorm.DB, q pagination.Query) *gorm.DB {
	if q.Status != "" {
		db = db.Where("status = ?", q.Status)
	}
	if q.Keyword != "" {
		like := "%" + q.Keyword + "%"
		db = db.Where("name LIKE ? OR code LIKE ? OR description LIKE ?", like, like, like)
	}
	return db
}

func (r *roleRepository) List(ctx context.Context, q pagination.Query) ([]model.Role, int64, error) {
	var total int64
	countDB := applyRoleFilters(r.db.WithContext(ctx).Session(&gorm.Session{}), q)
	if err := countDB.Model(&model.Role{}).Count(&total).Error; err != nil {
		return nil, 0, err
	}
	var roles []model.Role
	listDB := applyRoleFilters(r.db.WithContext(ctx).Session(&gorm.Session{}), q)
	offset := (q.Page - 1) * q.Size
	if err := listDB.Offset(offset).Limit(q.Size).Find(&roles).Error; err != nil {
		return nil, 0, err
	}
	return roles, total, nil
}

// ListAll 不分页返全量（按 status/keyword 过滤后的全切片），导出用。
func (r *roleRepository) ListAll(ctx context.Context, q pagination.Query) ([]model.Role, error) {
	var roles []model.Role
	db := applyRoleFilters(r.db.WithContext(ctx), q)
	if err := db.Find(&roles).Error; err != nil {
		return nil, err
	}
	return roles, nil
}

func (r *roleRepository) FindByID(ctx context.Context, id uint) (*model.Role, error) {
	var role model.Role
	if err := r.db.WithContext(ctx).First(&role, id).Error; err != nil {
		return nil, err
	}
	return &role, nil
}

func (r *roleRepository) Create(ctx context.Context, role *model.Role) error {
	return r.db.WithContext(ctx).Create(role).Error
}

func (r *roleRepository) Update(ctx context.Context, role *model.Role) error {
	return r.db.WithContext(ctx).Save(role).Error
}

// Delete 事务内软删角色 + 解除 role_permissions 与 user_roles 关联。
// User 未声明反向给 Role 的关联，故 user_roles 用 raw SQL 清。
func (r *roleRepository) Delete(ctx context.Context, id uint) error {
	return r.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
		if err := tx.Delete(&model.Role{}, id).Error; err != nil {
			return err
		}
		if err := tx.Where("role_id = ?", id).Delete(&struct{}{}).Table("role_permissions").Error; err != nil {
			return err
		}
		if err := tx.Where("role_id = ?", id).Delete(&struct{}{}).Table("user_roles").Error; err != nil {
			return err
		}
		return nil
	})
}

// BatchDelete 批量软删 + 解除关联。
func (r *roleRepository) BatchDelete(ctx context.Context, ids []uint) error {
	return r.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
		if err := tx.Delete(&model.Role{}, ids).Error; err != nil {
			return err
		}
		if err := tx.Where("role_id IN ?", ids).Delete(&struct{}{}).Table("role_permissions").Error; err != nil {
			return err
		}
		if err := tx.Where("role_id IN ?", ids).Delete(&struct{}{}).Table("user_roles").Error; err != nil {
			return err
		}
		return nil
	})
}

// GetRolePermissionCodes 查角色已分配的权限 code 数组。
func (r *roleRepository) GetRolePermissionCodes(ctx context.Context, roleID uint) ([]string, error) {
	var codes []string
	err := r.db.WithContext(ctx).
		Raw(`SELECT DISTINCT p.code FROM permissions p
			JOIN role_permissions rp ON rp.permission_id = p.id
			WHERE rp.role_id = ? AND p.deleted_at IS NULL`, roleID).
		Scan(&codes).Error
	return codes, err
}

// ReplaceRolePermissions 全量替换角色权限（Association Replace）。
func (r *roleRepository) ReplaceRolePermissions(ctx context.Context, roleID uint, permissionIDs []uint) error {
	return r.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {
		var role model.Role
		if err := tx.First(&role, roleID).Error; err != nil {
			return err
		}
		var perms []model.Permission
		if len(permissionIDs) > 0 {
			if err := tx.Where("id IN ?", permissionIDs).Find(&perms).Error; err != nil {
				return err
			}
		}
		return tx.Model(&role).Association("Permissions").Replace(&perms)
	})
}

// FindPermissionIDsByCodes 按 code 批量查 id，返回存在的 code→id 映射（未知 code 不在 map）。
func (r *roleRepository) FindPermissionIDsByCodes(ctx context.Context, codes []string) (map[string]uint, error) {
	m := make(map[string]uint)
	if len(codes) == 0 {
		return m, nil
	}
	var perms []model.Permission
	if err := r.db.WithContext(ctx).Where("code IN ?", codes).Find(&perms).Error; err != nil {
		return nil, err
	}
	for _, p := range perms {
		m[p.Code] = p.ID
	}
	return m, nil
}
```
> 注：Delete 的 `Delete(&struct{}{}).Table(...)` 写法为清空关联表记录。若 GORM 不接受 `&struct{}{}`，改用 `tx.Exec("DELETE FROM role_permissions WHERE role_id = ?", id)`。Step 4 测试会验证，按报错调整。

- [ ] **Step 4: 跑测试确认 GREEN**

Run: `cd server && go test ./internal/repository/ -run TestRoleRepo -v`
Expected: 4 测试 PASS。若 Delete 的 raw 写法报错，按报错改为 `tx.Exec("DELETE FROM ...")`，重跑。

- [ ] **Step 5: 准备提交（等用户确认）**

```bash
git add internal/repository/role.go internal/repository/role_test.go
git commit -m "feat(repo): RoleRepository 接口与 gorm 实现（含权限分配/解除关联）"
```

---

### Task 4: RoleService（TDD）

**Files:**
- Create: `server/internal/service/role.go`
- Create: `server/internal/service/role_test.go`

**Interfaces:**
- Consumes: `repository.RoleRepository`、`pagination`、`csvutil`、`apperr`、`middleware.InvalidateAll`、`isDuplicateKey`（同包，M3.1 已有）。
- Produces: `RoleService` + `NewRoleService(repo)`，方法 List/Get/Create/Update/Delete/BatchDelete/Export/GetPermissions/SetPermissions。

- [ ] **Step 1: 写失败测试**

创建 `server/internal/service/role_test.go`：
```go
package service

import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"gva/internal/model"
	"gva/internal/pkg/apperr"
	"gva/internal/pkg/pagination"
	"gva/internal/repository"
	"gva/internal/testutil"
)

func newRoleSvc(t *testing.T) (*RoleService, *repository.roleRepository) {
	// 见下方说明：改用接口返回不便测关联，直接返回 repo 接口类型
	return nil, nil
}

func TestRoleService_Create(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	r, err := svc.Create(ctx, "角色1", "r1", "描述", "active")
	require.NoError(t, err)
	assert.NotZero(t, r.ID)
}

func TestRoleService_Create_DuplicateCode(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	svc.Create(ctx, "n", "dup", "", "active")
	_, err := svc.Create(ctx, "n2", "dup", "", "active")
	require.Error(t, err)
	e, ok := apperr.As(err)
	require.True(t, ok)
	assert.Equal(t, 409, e.Status)
}

func TestRoleService_Get_NotFound(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	_, err := svc.Get(context.Background(), 9999)
	require.Error(t, err)
	e, _ := apperr.As(err)
	assert.Equal(t, 404, e.Status)
}

func TestRoleService_List(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	for i := 0; i < 12; i++ {
		svc.Create(ctx, "n", "c", "", "active")
	}
	q := pagination.Query{Page: 1, Size: 10}
	q.Normalize()
	res, err := svc.List(ctx, q)
	require.NoError(t, err)
	assert.Equal(t, int64(12), res.Total)
	assert.Len(t, res.Records, 10)
}

func TestRoleService_Delete_ClearsAssociations(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	r, _ := svc.Create(ctx, "n", "r1", "", "active")
	// 关联权限
	perm := &model.Permission{Code: "p1", Name: "p", Type: "api", Status: "active"}
	db.Create(perm)
	db.Model(&model.Role{Model: model.Model{ID: r.ID}}).Association("Permissions").Replace(perm)

	require.NoError(t, svc.Delete(ctx, r.ID))
	_, err := svc.Get(ctx, r.ID)
	assert.Error(t, err) // 软删后 404
}

func TestRoleService_SetPermissions_UnknownCode(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	r, _ := svc.Create(ctx, "n", "r1", "", "active")
	err := svc.SetPermissions(ctx, r.ID, []string{"unknown"})
	require.Error(t, err)
	e, _ := apperr.As(err)
	assert.Equal(t, 404, e.Status)
}

func TestRoleService_SetPermissions_Success(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	r, _ := svc.Create(ctx, "n", "r1", "", "active")
	perm := &model.Permission{Code: "p1", Name: "p", Type: "api", Status: "active"}
	db.Create(perm)
	require.NoError(t, svc.SetPermissions(ctx, r.ID, []string{"p1"}))
	codes, err := svc.GetPermissions(ctx, r.ID)
	require.NoError(t, err)
	assert.Contains(t, codes, "p1")
}

func TestRoleService_Export(t *testing.T) {
	db := testutil.NewTestDB(t)
	repo := repository.NewRoleRepository(db)
	svc := NewRoleService(repo)
	ctx := context.Background()
	svc.Create(ctx, "管理员", "r1", "描述", "active")
	q := pagination.Query{}
	q.Normalize()
	csv, err := svc.Export(ctx, q)
	require.NoError(t, err)
	assert.Contains(t, csv, "code")
	assert.Contains(t, csv, "r1")
}
```
删除占位的 `newRoleSvc` 函数（每测试内联构造）。

- [ ] **Step 2: 跑测试确认 RED**

Run: `cd server && go test ./internal/service/ -run TestRoleService -v`
Expected: 编译失败（RoleService 未定义）。

- [ ] **Step 3: 实现 role.go**

创建 `server/internal/service/role.go`：
```go
package service

import (
	"context"
	"errors"
	"strconv"

	"gorm.io/gorm"
	"gva/internal/middleware"
	"gva/internal/model"
	"gva/internal/pkg/apperr"
	"gva/internal/pkg/csvutil"
	"gva/internal/pkg/pagination"
	"gva/internal/repository"
)

// RoleService 角色业务。
type RoleService struct {
	repo repository.RoleRepository
}

func NewRoleService(repo repository.RoleRepository) *RoleService {
	return &RoleService{repo: repo}
}

// List 分页列表。
func (s *RoleService) List(ctx context.Context, q pagination.Query) (pagination.Result[model.Role], error) {
	q.Normalize()
	roles, total, err := s.repo.List(ctx, q)
	if err != nil {
		return pagination.Result[model.Role]{}, err
	}
	return pagination.Result[model.Role]{
		Records: roles, Total: total, Current: q.Page, Size: q.Size,
	}, nil
}

// Get 详情。不存在→404。
func (s *RoleService) Get(ctx context.Context, id uint) (*model.Role, error) {
	r, err := s.repo.FindByID(ctx, id)
	if err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, apperr.NotFound("角色不存在")
		}
		return nil, err
	}
	return r, nil
}

// Create 创建。code 重复→409。
func (s *RoleService) Create(ctx context.Context, name, code, description, status string) (*model.Role, error) {
	r := &model.Role{Name: name, Code: code, Description: description, Status: status}
	if err := s.repo.Create(ctx, r); err != nil {
		if isDuplicateKey(err) {
			return nil, apperr.Conflict("角色编码已存在")
		}
		return nil, err
	}
	return r, nil
}

// Update 更新。
func (s *RoleService) Update(ctx context.Context, r *model.Role) error {
	if err := s.repo.Update(ctx, r); err != nil {
		if isDuplicateKey(err) {
			return apperr.Conflict("角色编码已存在")
		}
		return err
	}
	return nil
}

// Delete 软删+解除关联，失效权限缓存。
func (s *RoleService) Delete(ctx context.Context, id uint) error {
	if _, err := s.repo.FindByID(ctx, id); err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return apperr.NotFound("角色不存在")
		}
		return err
	}
	if err := s.repo.Delete(ctx, id); err != nil {
		return err
	}
	middleware.InvalidateAll()
	return nil
}

// BatchDelete 批量软删+解除关联。
func (s *RoleService) BatchDelete(ctx context.Context, ids []uint) error {
	if err := s.repo.BatchDelete(ctx, ids); err != nil {
		return err
	}
	middleware.InvalidateAll()
	return nil
}

// Export 生成 CSV。用 ListAll 取全量，避免分页漏数据。
func (s *RoleService) Export(ctx context.Context, q pagination.Query) (string, error) {
	roles, err := s.repo.ListAll(ctx, q)
	if err != nil {
		return "", err
	}
	rows := make([]map[string]any, 0, len(roles))
	for _, r := range roles {
		rows = append(rows, map[string]any{
			"id":          strconv.FormatUint(uint64(r.ID), 10),
			"name":        r.Name,
			"code":        r.Code,
			"description": r.Description,
			"status":      r.Status,
			"createTime":  r.CreatedAt,
			"updateTime":  r.UpdatedAt,
		})
	}
	return csvutil.Build(rows, []string{"id", "name", "code", "description", "status", "createTime", "updateTime"}), nil
}

// GetPermissions 查角色已分配的权限 code 数组。角色不存在→404。
func (s *RoleService) GetPermissions(ctx context.Context, roleID uint) ([]string, error) {
	if _, err := s.repo.FindByID(ctx, roleID); err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return nil, apperr.NotFound("角色不存在")
		}
		return nil, err
	}
	return s.repo.GetRolePermissionCodes(ctx, roleID)
}

// SetPermissions 全量替换角色权限（API 用 code，表存 id）。
// 严格校验：所有 code 必须存在，未知→404。
func (s *RoleService) SetPermissions(ctx context.Context, roleID uint, codes []string) error {
	if _, err := s.repo.FindByID(ctx, roleID); err != nil {
		if errors.Is(err, gorm.ErrRecordNotFound) {
			return apperr.NotFound("角色不存在")
		}
		return err
	}
	codeToID, err := s.repo.FindPermissionIDsByCodes(ctx, codes)
	if err != nil {
		return err
	}
	// 严格校验：未知 code 报错
	for _, code := range codes {
		if _, ok := codeToID[code]; !ok {
			return apperr.NotFound("权限 " + code + " 不存在")
		}
	}
	ids := make([]uint, 0, len(codes))
	for _, code := range codes {
		ids = append(ids, codeToID[code])
	}
	if err := s.repo.ReplaceRolePermissions(ctx, roleID, ids); err != nil {
		return err
	}
	middleware.InvalidateAll()
	return nil
}
```

- [ ] **Step 4: 跑测试确认 GREEN**

Run: `cd server && go test ./internal/service/ -run TestRoleService -v`
Expected: 8 测试 PASS。

- [ ] **Step 5: 全包回归**

Run: `cd server && go test ./internal/service/ -count=1`
Expected: M2+M3.1+M3.2 service 测试全 PASS。

- [ ] **Step 6: 准备提交（等用户确认）**

```bash
git add internal/service/role.go internal/service/role_test.go
git commit -m "feat(service): RoleService CRUD+list+export+权限分配"
```

---

### Task 5: RoleHandler + 路由 + main 组装

**Files:**
- Create: `server/internal/handler/role.go`
- Modify: `server/internal/server/router.go`
- Modify: `server/cmd/api/main.go`

**Interfaces:**
- Consumes: `*service.RoleService`、`middleware.AuthRequired`+`RequirePermission`、`apperr`、`response`、`pagination`。
- Produces: 9 端点可运行。

- [ ] **Step 1: 实现 handler/role.go**

创建 `server/internal/handler/role.go`：
```go
package handler

import (
	"strconv"

	"github.com/gin-gonic/gin"
	"gva/internal/middleware"
	"gva/internal/pkg/apperr"
	"gva/internal/pkg/pagination"
	"gva/internal/pkg/response"
	"gva/internal/service"
)

type roleCreateReq struct {
	Name        string `json:"name" binding:"required"`
	Code        string `json:"code" binding:"required"`
	Description string `json:"description"`
	Status      string `json:"status" binding:"required,oneof=active inactive"`
}

type setPermissionsReq struct {
	Permissions []string `json:"permissions" binding:"required"`
}

type batchDeleteReq struct {
	IDs []string `json:"ids" binding:"required,min=1"`
}

type RoleHandler struct {
	svc *service.RoleService
}

func NewRoleHandler(svc *service.RoleService) *RoleHandler {
	return &RoleHandler{svc: svc}
}

// List GET /api/role
func (h *RoleHandler) List(c *gin.Context) {
	var q pagination.Query
	if err := c.ShouldBindQuery(&q); err != nil {
		apperr.Write(c, apperr.Validation(err.Error(), nil))
		return
	}
	res, err := h.svc.List(c.Request.Context(), q)
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, res)
}

// Export GET /api/role/export
func (h *RoleHandler) Export(c *gin.Context) {
	var q pagination.Query
	_ = c.ShouldBindQuery(&q)
	csv, err := h.svc.Export(c.Request.Context(), q)
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, csv, "导出成功")
}

// Get GET /api/role/:id
func (h *RoleHandler) Get(c *gin.Context) {
	id, err := strconv.ParseUint(c.Param("id"), 10, 64)
	if err != nil {
		apperr.Write(c, apperr.Validation("无效的 id", nil))
		return
	}
	r, err := h.svc.Get(c.Request.Context(), uint(id))
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, r)
}

// Create POST /api/role
func (h *RoleHandler) Create(c *gin.Context) {
	var req roleCreateReq
	if err := c.ShouldBindJSON(&req); err != nil {
		apperr.Write(c, apperr.Validation(err.Error(), nil))
		return
	}
	r, err := h.svc.Create(c.Request.Context(), req.Name, req.Code, req.Description, req.Status)
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, r, "创建成功")
}

// Update PUT /api/role/:id
func (h *RoleHandler) Update(c *gin.Context) {
	id, err := strconv.ParseUint(c.Param("id"), 10, 64)
	if err != nil {
		apperr.Write(c, apperr.Validation("无效的 id", nil))
		return
	}
	var req roleCreateReq
	if err := c.ShouldBindJSON(&req); err != nil {
		apperr.Write(c, apperr.Validation(err.Error(), nil))
		return
	}
	r, err := h.svc.Get(c.Request.Context(), uint(id))
	if err != nil {
		apperr.Write(c, err)
		return
	}
	r.Name = req.Name
	r.Code = req.Code
	r.Description = req.Description
	r.Status = req.Status
	if err := h.svc.Update(c.Request.Context(), r); err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, r, "更新成功")
}

// Delete DELETE /api/role/:id
func (h *RoleHandler) Delete(c *gin.Context) {
	id, err := strconv.ParseUint(c.Param("id"), 10, 64)
	if err != nil {
		apperr.Write(c, apperr.Validation("无效的 id", nil))
		return
	}
	if err := h.svc.Delete(c.Request.Context(), uint(id)); err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, true, "删除成功")
}

// BatchDelete DELETE /api/role
func (h *RoleHandler) BatchDelete(c *gin.Context) {
	var req batchDeleteReq
	if err := c.ShouldBindJSON(&req); err != nil {
		apperr.Write(c, apperr.Validation(err.Error(), nil))
		return
	}
	ids := make([]uint, 0, len(req.IDs))
	for _, s := range req.IDs {
		id, err := strconv.ParseUint(s, 10, 64)
		if err != nil {
			apperr.Write(c, apperr.Validation("无效的 id: "+s, nil))
			return
		}
		ids = append(ids, uint(id))
	}
	if err := h.svc.BatchDelete(c.Request.Context(), ids); err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, true, "删除成功")
}

// GetPermissions GET /api/role/:id/permissions
func (h *RoleHandler) GetPermissions(c *gin.Context) {
	id, err := strconv.ParseUint(c.Param("id"), 10, 64)
	if err != nil {
		apperr.Write(c, apperr.Validation("无效的 id", nil))
		return
	}
	codes, err := h.svc.GetPermissions(c.Request.Context(), uint(id))
	if err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, codes)
}

// SetPermissions PUT /api/role/:id/permissions
func (h *RoleHandler) SetPermissions(c *gin.Context) {
	id, err := strconv.ParseUint(c.Param("id"), 10, 64)
	if err != nil {
		apperr.Write(c, apperr.Validation("无效的 id", nil))
		return
	}
	var req setPermissionsReq
	if err := c.ShouldBindJSON(&req); err != nil {
		apperr.Write(c, apperr.Validation(err.Error(), nil))
		return
	}
	if err := h.svc.SetPermissions(c.Request.Context(), uint(id), req.Permissions); err != nil {
		apperr.Write(c, err)
		return
	}
	response.Success(c, true, "设置成功")
}
```
> 注：`batchDeleteReq` 与 permission handler 的同名（M3.1 已有）。若同包冲突，重命名为 `roleBatchDeleteReq`。检查 `internal/handler/permission.go` 是否已有 `batchDeleteReq`——若有，本文件改用 `roleBatchDeleteReq`。

- [ ] **Step 2: 修改 router.go 注册角色路由**

修改 `server/internal/server/router.go`：
1. 签名加 `roleHandler`：
```go
func NewRouter(authHandler *handler.AuthHandler, permHandler *handler.PermissionHandler, roleHandler *handler.RoleHandler, permRepo repository.PermissionRepository, jwtMgr *jwt.Manager) *gin.Engine {
```
2. 在 permission 路由组之后追加 role 路由组：
```go
	// 角色管理
	role := api.Group("/role")
	role.Use(middleware.AuthRequired(jwtMgr))
	{
		role.GET("/export", middleware.RequirePermission(permRepo, "role:list"), roleHandler.Export)
		role.GET("/:id/permissions", middleware.RequirePermission(permRepo, "role:permission"), roleHandler.GetPermissions)
		role.PUT("/:id/permissions", middleware.RequirePermission(permRepo, "role:permission"), roleHandler.SetPermissions)
		role.GET("", middleware.RequirePermission(permRepo, "role:list"), roleHandler.List)
		role.GET("/:id", middleware.RequirePermission(permRepo, "role:list"), roleHandler.Get)
		role.POST("", middleware.RequirePermission(permRepo, "role:create"), roleHandler.Create)
		role.PUT("/:id", middleware.RequirePermission(permRepo, "role:edit"), roleHandler.Update)
		role.DELETE("/:id", middleware.RequirePermission(permRepo, "role:delete"), roleHandler.Delete)
		role.DELETE("", middleware.RequirePermission(permRepo, "role:delete"), roleHandler.BatchDelete)
	}
```
> 路由顺序：`/export`、`/:id/permissions` 在 `/:id` 前注册，确保静态/子路径优先。

- [ ] **Step 3: 修改 main.go 组装**

修改 `server/cmd/api/main.go`：
1. 在 permHandler 组装后追加：
```go
	roleRepo := repository.NewRoleRepository(gdb)
	roleSvc := service.NewRoleService(roleRepo)
	roleHandler := handler.NewRoleHandler(roleSvc)
```
2. NewRouter 调用改为：
```go
	r := server.NewRouter(authHandler, permHandler, roleHandler, permRepo, jwtMgr)
```

- [ ] **Step 4: 编译验证**

Run: `cd server && go build ./...`
Expected: 退出码 0。若 `batchDeleteReq` 冲突，按 Step 1 注释重命名。

- [ ] **Step 5: 端到端验收**

启动服务并验收：
```bash
cd server
go run cmd/api/main.go > /tmp/gva-m32.log 2>&1 &
sleep 5
TOKEN=$(curl -s -X POST http://127.0.0.1:8088/api/auth/sessions -H 'Content-Type: application/json' -d '{"username":"admin","password":"123456"}' | grep -o '"accessToken":"[^"]*"' | cut -d'"' -f4)
# 1. 列表（admin 持 * 放行）
curl -s "http://127.0.0.1:8088/api/role?page=1&size=5" -H "Authorization: Bearer $TOKEN"
# 2. 创建角色
curl -s -X POST http://127.0.0.1:8088/api/role -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"name":"测试角色","code":"test_role","description":"d","status":"active"}'
# 3. 重复 code → 409
curl -s -X POST http://127.0.0.1:8088/api/role -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"name":"x","code":"test_role","status":"active"}'
# 4. 取角色ID后查权限
RID=$(curl -s "http://127.0.0.1:8088/api/role?page=1&size=5" -H "Authorization: Bearer $TOKEN" | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2)
curl -s "http://127.0.0.1:8088/api/role/$RID/permissions" -H "Authorization: Bearer $TOKEN"
# 5. 设置权限（含已知 user:read）
curl -s -X PUT "http://127.0.0.1:8088/api/role/$RID/permissions" -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"permissions":["user:read"]}'
# 6. 设置未知权限 → 404
curl -s -X PUT "http://127.0.0.1:8088/api/role/$RID/permissions" -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"permissions":["unknown_code"]}'
# 7. 导出
curl -s "http://127.0.0.1:8088/api/role/export" -H "Authorization: Bearer $TOKEN"
# 8. user 访问 → 403
UTOKEN=$(curl -s -X POST http://127.0.0.1:8088/api/auth/sessions -H 'Content-Type: application/json' -d '{"username":"user","password":"123456"}' | grep -o '"accessToken":"[^"]*"' | cut -d'"' -f4)
curl -s "http://127.0.0.1:8088/api/role?page=1" -H "Authorization: Bearer $UTOKEN"
```
Expected:
1. 列表分页，含 super_admin/user 种子角色
2. 创建成功
3. 409 "角色编码已存在"
4. 权限 code 数组（可能空）
5. 设置成功，msg="设置成功"
6. 404 "权限 unknown_code 不存在"
7. csv 文本
8. user → 403
日志含"种子数据就绪"。

- [ ] **Step 6: 停服务**

```bash
pkill -f "cmd/api/main.go"; pkill -f "exe/main" 2>/dev/null; echo done
```

- [ ] **Step 7: 全包测试回归**

Run: `cd server && go test ./... -count=1`
Expected: 全 PASS（M2+M3.1+M3.2）。

- [ ] **Step 8: 准备提交（等用户确认）**

```bash
git add internal/handler/role.go internal/server/router.go cmd/api/main.go
git commit -m "feat(role): handler + 路由 + 组装（M3.2 完成）"
```

---

## Self-Review

**Spec coverage:** 逐项核对 spec 第 11 节验收标准：
- go build → Task 5 Step 4 ✓
- go test ./... 全过 → Task 5 Step 7 ✓
- admin 列表 → Task 5 Step 5 #1 ✓
- user 403 → Task 5 Step 5 #8 ✓
- 创建+重复409 → Task 5 Step 5 #2/#3 ✓
- 删除软删解除关联 → Task 4 测试 + Task 5 #（删） ✓
- GET 权限 → Task 5 Step 5 #4 ✓
- PUT 权限+未知404 → Task 5 Step 5 #5/#6 ✓
- 导出 csv → Task 5 Step 5 #7 ✓
- 种子 Description → Task 2 ✓

**Placeholder scan:** 无 TBD/TODO；Task 4 的 newRoleSvc 占位在 Step 1 说明删除、内联构造。

**Type consistency:**
- `NewRoleService(repo)` —— Task 4 定义，Task 5 调用一致 ✓
- `NewRoleRepository(db)` —— Task 3 定义，Task 4/5 调用一致 ✓
- `NewRouter` 签名加 roleHandler —— Task 5 Step 2 定义，Step 3 调用一致 ✓
- `seedRole(ctx, code, name, description, perms)` —— Task 2 定义，调用一致 ✓
- `middleware.InvalidateAll` —— M3.1 已定义，Task 4 service 调用 ✓
- `isDuplicateKey` —— M3.1 service 包已有，Task 4 同包复用 ✓

**潜在风险（非阻断，标注）：**
1. Task 3 Delete 的 `Delete(&struct{}{}).Table(...)` 写法 GORM 可能不接受空结构体。备选 `tx.Exec("DELETE FROM role_permissions WHERE role_id = ?", id)`。Step 4 测试会暴露，按报错切换。
2. Task 5 `batchDeleteReq` 与 permission handler 同名冲突风险——Step 1 已标注重命名为 `roleBatchDeleteReq`。
3. Gin 路由 `/:id/permissions` 与 `/:id` 共存——子路径独立，Gin 树形路由支持，但需 `/export`、`/:id/permissions` 先注册（Step 2 已按此顺序）。
4. Task 2 FirstOrCreate 后额外 Update 补 Description——对已存在角色每次启动都 Update 一次（幂等，无副作用），可接受。
5. Task 4 Export 已用 `repo.ListAll`（Task 3 新增）取全量不分页，避免分页漏数据，与 M3.1 permission Export 一致。
