# pnpm 配置（docs-site 独立项目）。
# packages: ['.']：声明自身为 workspace 包。pnpm 9 要求此字段非空（否则 "packages field missing"），
#   pnpm 11 可省略。docs-site 无子项目，此声明不影响仓库根或 web/。
# allowBuilds：v11 替代 onlyBuiltDependencies，显式允许 esbuild 构建脚本（vitepress 依赖）。
packages:
  - '.'
allowBuilds:
  esbuild: true
